# Overview
Contains the TODOs that are not entire features, but improvements.
1. Use https://api.central.ballerina.io/2.0/docs/<org>/<name>/<version> to get the docs instead of scraping the website
2. Incorporate ballerina.io docs (using the git repo) into the RAG
